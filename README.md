# vela
Vela is an AI-powered API marketplace agent that autonomously purchases real data services using HBAR, with a deterministic policy layer enforcing spend limits, vendor allowlists, and daily budgets at runtime.
